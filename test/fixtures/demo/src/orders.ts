// Fixture realization file: the orders square's `src/orders*` selector matches
// it and its `expect: annotated` coverage check is satisfied by the anchor.
// @sq orders#concept/order -- placement through fulfillment lives here
export const placeholder = true;
